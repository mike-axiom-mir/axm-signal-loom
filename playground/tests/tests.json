[
  {
    "name": "Enable every organ without filling every input",
    "sanity": true,
    "viewport": "desktop",
    "covers": [
      "non-blocking empty source organs",
      "waiting source state",
      "weighted capture recipe",
      "visual proposal generation"
    ],
    "description": "Verifies enabling all source organs does not turn empty optional sources into capture errors.",
    "steps": [
      "Enable every source organ without filling the optional empty JSON/provenance fields",
      "Press \"Capture + weave\"",
      "Inspect the frozen Influence receipt and proposal grid"
    ],
    "expected": "Capture succeeds using the source organs that actually have usable signals; empty enabled organs remain marked as waiting and do not appear in the frozen influence receipt."
  },
  {
    "name": "Load sample signals across the whole machine",
    "viewport": "desktop",
    "covers": [
      "sample signal fixtures",
      "all source-organ usability",
      "influence receipt"
    ],
    "description": "Verifies the whole source registry can be explored immediately without requiring external accounts or data.",
    "steps": [
      "Press \"Load sample signals\"",
      "Press \"Capture + weave\"",
      "Inspect the Influence receipt"
    ],
    "expected": "All ten source organs have usable demo fixtures, capture succeeds, and the receipt visibly includes all ten weighted influences while identifying the samples as demo fixtures rather than live sources."
  },
  {
    "name": "Internet Eye reduces observations before exposure",
    "viewport": "desktop",
    "covers": [
      "native Internet Eye organ",
      "target-blind reduction",
      "provenance preservation"
    ],
    "description": "Verifies raw Shodan-compatible observations are reduced before the frozen session is exposed.",
    "steps": [
      "Enable Internet Eye",
      "Enter {\"matches\":[{\"ip_str\":\"203.0.113.55\",\"hostnames\":[\"hidden.invalid\"],\"port\":443,\"transport\":\"tcp\",\"location\":{\"country_code\":\"NL\"},\"product\":\"nginx\",\"org\":\"Example Org\",\"data\":\"private banner\",\"ssl\":{\"enabled\":true}}]} in its JSON input",
      "Enter \"QA indexed fixture\" as provenance and press \"Capture + weave\", then open \"Inspect frozen source packets\""
    ],
    "expected": "Internet Eye is present as target-blind aggregate influence, while the raw IP, hostname, organization name, and banner text are not visible in the frozen session JSON."
  },
  {
    "name": "Entered external data still requires provenance",
    "viewport": "desktop",
    "covers": [
      "provenance guardrail",
      "failed capture preservation",
      "no silent rewrite"
    ],
    "description": "Verifies waiting empty sources are harmless but an external packet that is actually supplied must carry provenance.",
    "steps": [
      "Enable Connector Packet",
      "Enter {\"signal\":0.5} in its JSON input but leave provenance empty",
      "Press \"Capture + weave\""
    ],
    "expected": "A visible error says Connector Packet requires provenance and no new session receipt replaces the prior frozen session."
  },
  {
    "name": "Explicitly save and reload a frozen session on mobile",
    "viewport": "mobile",
    "covers": [
      "explicit local session save",
      "session reload",
      "mobile machine controls"
    ],
    "description": "Verifies persistence occurs only after the user explicitly requests it.",
    "steps": [
      "Press \"Capture + weave\"",
      "Press \"Save session locally\"",
      "Press \"Load saved session\""
    ],
    "expected": "The frozen session remains visible with the same session fingerprint and influence receipt after explicit reload."
  }
]